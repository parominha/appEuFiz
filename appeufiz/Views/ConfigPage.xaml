<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="appeufiz.Views.ConfigPage"
             xmlns:vm="clr-namespace:appeufiz.ViewModels"
             BackgroundColor="#1A1A1A">

    <ContentPage.BindingContext>
        <vm:ConfigViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
      <StackLayout>
            <Label Text="CONFIGURAÇÕES" TextColor="White" FontSize="28" HorizontalOptions="CenterAndExpand" Padding="0,30,0,50"/>

            <StackLayout Orientation="Vertical" Padding="30,0,30,24">
                <Button x:Name="btnConta" Margin="0,0,0,15" Text="Minha conta" 
                        BackgroundColor="#333333"
                        TextColor="White"
                        CornerRadius = "25"
                        Clicked="btnConta_Clicked"
                        FontSize="16"/>
                <Button x:Name="btnBackup" Margin="0,10,0,15" Text="Backup" 
                        BackgroundColor="#333333"
                        TextColor="White"
                        CornerRadius = "25"
                        Clicked="btnBackup_Clicked"
                        FontSize="16"/>
                <Button x:Name="btnLogout" Margin="0,10,0,15" Text="Logout" 
                        BackgroundColor="#333333"
                        TextColor="White"
                        CornerRadius = "25"
                        Clicked="btnLogout_Clicked"
                        FontSize="16"/>
            </StackLayout>
        </StackLayout>
  </ContentPage.Content>
</ContentPage>